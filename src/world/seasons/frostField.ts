/**
 * How much frost lies on the world, 0 to 1, as the shaders read it. Written by
 * `Seasons` every step, read by `FrostPlugin` on every surface it is on.
 */
export const frostField = { amount: 0 };
