/** The hole the fire sits in. */
export const OPENING_WIDTH = 1.15;
export const OPENING_HEIGHT = 1.2;
/** The stone either side of the opening, and the beam over it. */
export const JAMB_WIDTH = 0.32;
export const LINTEL_HEIGHT = 0.3;
/** How far the surround stands out from the wall into the room. */
export const SURROUND_DEPTH = 0.55;
/** The slab on the floor, which reaches further out than the surround. */
export const HEARTH_DEPTH = 0.8;
export const HEARTH_HEIGHT = 0.09;
/** The stack outside, and how far it clears the ridge. */
export const STACK_WIDTH = 1.15;
export const STACK_DEPTH = 0.75;
export const STACK_ABOVE_RIDGE = 1.2;
export const CAP_HEIGHT = 0.16;
